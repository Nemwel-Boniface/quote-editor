en:
  simple_form:
    "yes": 'Yes'
    "no": 'No'
    required:
      text: 'required'
      mark: '*'
      # html: '<abbr title="required">*</abbr>'

    placeholders:
      quote:
        name: "Name of your quote"

    labels:               # Moved this to the correct level under `simple_form`
      quote:
        name: "Name"

    error_notification:
      default_message: "Please review the problems below:"

  helpers:                # `helpers` should be outside `simple_form`
    submit:
      quote:
        create: "Create quote"
        update: "Update quote"
